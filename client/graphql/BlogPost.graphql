query BlogPost($id: ID!) {
  blogPost(id: $id) {
    data {
      id
      attributes {
        title
        description
        content
        author {
          data {
            attributes {
              name
              link
            }
          }
        }
      }
    }
  }
}
