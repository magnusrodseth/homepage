# Strapi application

A quick description of your strapi application

When writing the actual README, add the `npm dedupe` may soleve the following issue: `TypeError: The 'compilation' argument must be an instance of Compilation`.
